# Public environment variables for the frontend
# The orchestrator will set these in the actual .env; do not commit real values here.

# Base URL of the backend FastAPI service.
# Defaults to http://localhost:3001 if not set.
REACT_APP_BACKEND_URL=http://localhost:3001
